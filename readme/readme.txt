Models.py:

S'utilitza per definir els models de la base de dades, em mantingut la mateixa que a la practica 1.


Views.py:

Aquest archiu defineix les vistes, que son funcions o clases que controlen la logica i el comportament web de las solicituds web en l'aplicació.

Tot funciona amb el ApiRest, solament li tens que dir el model i ja s'encarrega ell.

